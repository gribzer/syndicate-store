# --- Общие логи и временные файлы Node.js ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Instrumented libs (jscoverage/JSCover)
lib-cov

# Coverage directories
coverage
*.lcov
.nyc_output

# Grunt
.grunt

# Bower
bower_components

# node-gyp, node-waf
.lock-wscript

# Compiled addons
build/Release

# Dependency dirs
node_modules/
jspm_packages/
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm/yarn/pnpm caches
.npm
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# ESLint / stylelint cache
.eslintcache
.stylelintcache

# Microbundle / bundler caches
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/
.fusebox/
.parcel-cache
.cache/
.temp/

# REPL history
.node_repl_history

# npm pack / Yarn pack output
*.tgz

# Yarn integrity
.yarn-integrity

# dotenv files (не храним секреты в Git)
.env
.env.*.local
.env.local

# Serverless
.serverless/

# Mac / Windows system files
.DS_Store
Thumbs.db

# VSCode test
.vscode-test

# --- Next.js / React / Gatsby / Vue / Nuxt ---
.next
out
.nuxt
dist
.cache/
.vuepress/dist
.docusaurus

# Gatsby
.cache/
#public  # Если не используете Next.js public/

# --- Strapi ---
# В Strapi бывают временные каталоги:
.tmp/            # временные файлы (по умолчанию в Strapi)
build/           # если собираете Strapi админку
# uploads/       # Если не храните загруженные медиа в репо, можете раскомментировать

# --- Telegram Bot ---
# (Если храните .env и т. д. в корне бота, уже покрыто выше)

# --- Docker ---
# Можно игнорировать временные файлы, если таковые образуются
# Например, логи контейнеров, если они записываются в отдельную папку, etc.
# При этом Dockerfile / docker-compose.yml обычно оставляют в репозитории
# Добавьте при необходимости:
# docker/node_modules/   # если вы копируете node_modules внутрь docker при сборке
# docker/*.env           # если хотите скрыть .env в docker/

